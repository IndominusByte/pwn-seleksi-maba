#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdio.h>

void flag(){
char flag[20] = {'K','S','L','{','R','3','4','d','_','A','s','s','3','m','b','L','Y','}'};
char isi[20];
  for(int i = 0; i<4; i++){
    isi[i] = flag[i];
  }
}

int main(){
printf("where the flag ? \n");
}
